$(document).ready(function() {
  $('#user_task_due').datepicker({
    autoclose: true,
    todayHighlight: true,
    toggleActive: true,
    format: "yyyy-mm-dd",
    orientation: "bottom auto"
    });
});