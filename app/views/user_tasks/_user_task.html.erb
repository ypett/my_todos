<div class="well">
  <%= user_task.description %>
  <%= user_task.due %>
  <%= link_to "Edit", edit_user_task_path(user_task), remote: true, class: 'btn btn-warning' %>
  <%= link_to "Delete", user_task, remote: true, method: :delete, data: { confirm: "Are you sure you want to delete #{user_task.description}?" }, class: 'btn btn-danger' %>
</div>